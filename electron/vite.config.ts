// electron/vite.config.ts
/*import { defineConfig } from 'vite'
import electron from 'vite-plugin-electron'
import path from 'path'

export default defineConfig({
  plugins: [/*
    electron({
      entry: 'main.cjs',
      vite: {
        build: {
          outDir: 'dist',
          target: 'node18',
          sourcemap: true,
          rollupOptions: {
            input: path.resolve(__dirname, 'main.cjs'),
            external: [
              /^@duckdb\/node-bindings/,
              /\.node$/
            ]
          }
        }
      }
    })
  ]
})
*/